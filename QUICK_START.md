# Быстрый старт Backend

## Предварительные требования

- **Go 1.21+** - [Установка Go](https://golang.org/doc/install)
- **Docker и Docker Compose** - для PostgreSQL и Redis
- **Git** - для клонирования репозитория

## Шаг 1: Клонирование и установка зависимостей

```bash
# Если еще не клонировали репозиторий
git clone <repository-url>
cd <project-name>

# Установка зависимостей Go
cd backend
go mod download
```

## Шаг 2: Запуск инфраструктуры (PostgreSQL и Redis)

```bash
# Из корня проекта
docker-compose up -d

# Проверка что контейнеры запущены
docker ps
```

Должны быть запущены:
- PostgreSQL на порту 5432
- Redis на порту 6379

## Шаг 3: Настройка переменных окружения

```bash
cd backend

# Создать .env файл из примера
cp .env.example .env

# Отредактировать .env при необходимости
# Для локальной разработки можно оставить значения по умолчанию
```

**Важно:** В production обязательно измените `JWT_SECRET` на случайную строку!

## Шаг 4: Запуск backend сервера

### Вариант 1: Через Makefile (из корня проекта)

```bash
# Из корня проекта
make backend
```

### Вариант 2: Напрямую через Go

```bash
cd backend
go run cmd/server/main.go
```

### Вариант 3: Сборка и запуск бинарника

```bash
cd backend
go build -o bin/server cmd/server/main.go
./bin/server
```

## Шаг 5: Проверка работоспособности

После запуска сервера вы должны увидеть:

```
🚀 API Server starting on port 8080
🚀 gRPC Server starting on port 9090
WebSocket signaling server starting on port 8081
TURN server started on port 3478
```

### Проверка endpoints:

1. **Health check:**
```bash
curl http://localhost:8080/health
```

Ответ:
```json
{
  "status": "ok",
  "service": "Backend-api"
}
```

2. **Swagger UI:**
Откройте в браузере: http://localhost:8080/swagger/index.html

3. **Регистрация пользователя:**
```bash
curl -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "testpassword123"
  }'
```

## Что запускается

При запуске backend сервера стартуют:

1. **HTTP API Server** - порт 8080
   - REST API для фронтенда
   - Swagger UI: http://localhost:8080/swagger/index.html

2. **gRPC Server** - порт 9090
   - Внутренние сервисы (Auth, Device, File, Transfer)

3. **WebSocket Signaling Server** - порт 8081
   - WebRTC signaling для P2P соединений
   - Endpoint: `ws://localhost:8081/ws/signaling`

4. **TURN Server** - порт 3478
   - Ретрансляция WebRTC трафика
   - Используется как fallback для P2P

## Структура портов

| HTTP API | 8080 | REST API, Swagger UI |
| gRPC | 9090 | Внутренние сервисы |
| WebSocket | 8081 | WebRTC Signaling |
| TURN | 3478 | TURN сервер (UDP) |
| PostgreSQL | 5432 | База данных |
| Redis | 6379 | Кэш и сессии |

## Остановка серверов

Нажмите `Ctrl+C` для graceful shutdown всех серверов.

## Остановка Docker контейнеров

```bash
# Из корня проекта
docker-compose down

# Или с удалением данных
docker-compose down -v
```

## Troubleshooting

### Ошибка подключения к PostgreSQL

```bash
# Проверьте что контейнер запущен
docker ps | grep postgres

# Проверьте логи
docker logs Backend-postgres

# Перезапустите контейнер
docker-compose restart postgres
```

### Ошибка подключения к Redis

```bash
# Проверьте что контейнер запущен
docker ps | grep redis

# Проверьте логи
docker logs Backend-redis

# Перезапустите контейнер
docker-compose restart redis
```

### Порт уже занят

Если порт занят, измените его в `.env` файле или остановите процесс, использующий порт:

```bash
# Найти процесс на порту 8080
lsof -i :8080

# Убить процесс
kill -9 <PID>
```

### Ошибка миграций

```bash
# Проверьте подключение к БД
docker exec -it postgres psql -U user -d db -c "SELECT 1;"

# Если нужно пересоздать БД
docker-compose down -v
docker-compose up -d
```

## Следующие шаги

1. Откройте Swagger UI: http://localhost:8080/swagger/index.html
2. Зарегистрируйте пользователя через API
3. Получите JWT токен
4. Протестируйте endpoints через Swagger UI

## Полезные команды

```bash
# Показать все команды Makefile
make help

# Запустить все сервисы (Docker + Backend)
make dev

# Остановить Docker контейнеры
make docker-down

# Собрать backend
make backend-build

# Запустить тесты
make backend-test
```
